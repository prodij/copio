services:
  postgres:
    extends:
      file: docker-compose.yml
      service: postgres

  redis:
    extends:
      file: docker-compose.yml
      service: redis

  # Inventory service placeholder - uncomment when ready
  # inventory:
  #   build:
  #     context: ./services/inventory
  #     dockerfile: Dockerfile
  #   environment:
  #     DATABASE_URL: ${DATABASE_URL}
  #     REDIS_URL: ${REDIS_URL}
  #     PORT: ${PORT:-3001}
  #     NODE_ENV: development
  #   ports:
  #     - "${PORT:-3001}:${PORT:-3001}"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   volumes:
  #     - ./services/inventory:/app
  #     - /app/node_modules

volumes:
  postgres_data:
  redis_data:
